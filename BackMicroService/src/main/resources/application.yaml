spring:
  application:
    name: eventGestion
  datasource:
    url: jdbc:mysql://localhost:3306/eventdb?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8082




